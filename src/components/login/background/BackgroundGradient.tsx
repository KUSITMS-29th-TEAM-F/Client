const BackgroundGradient = () => {
  return (
    <div
      className="absolute left-0 top-0 -z-10 flex h-full w-full items-center justify-center opacity-30"
      style={{
        background: 'linear-gradient(137deg, #FFFAF0 47.9%, #FCDA8C 100%)',
      }}
    />
  );
};

export default BackgroundGradient;
